
function averageMark(a, g, p) {
	let algebra = a;
	let geometry = g;
	let physics = p;
	
    let averageRating = (a + g + p)/3; //среднее значение.
    
	return averageRating  // ответ;
}

function sayHello(userName) {
	let myName = userName;

	let message = ("Привет, мир! Меня зовут " + userName);
    
	return message // ответ;
}

function calculateFormula() {
    let игрек = 2;
	let у = 22;
	let зет = 0;

	// x * y + 5 * z + x - 1

	let result = игрек*у+5*зет+игрек-1;
    
    return result //  ответ;
}
